# Google Location History Analyzer - Environment Variables

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Database connection (PostgreSQL)
# Format: postgresql://username:password@host:port/database_name
# Example for local PostgreSQL: postgresql://postgres:password@localhost:5432/location_analyzer
# Example for Replit: Use the DATABASE_URL provided by Replit automatically
DATABASE_URL=postgresql://username:password@localhost:5432/location_analyzer

# Geoapify API Key (REQUIRED for geocoding coordinates to addresses)
# Get your free key at: https://www.geoapify.com/
# Free tier includes 3,000 requests per day
GEOAPIFY_API_KEY=your_geoapify_api_key_here

# Session secret for secure session management
# Generate a random string for production use
SESSION_SECRET=your_random_session_secret_here

# =============================================================================
# OPTIONAL VARIABLES (Enhanced Features)
# =============================================================================

# OpenAI API Key (OPTIONAL - for AI-powered location insights)
# Get your key at: https://platform.openai.com/api-keys
# If not provided, AI features will be disabled but core functionality works
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# AUTOMATIC VARIABLES (Provided by Replit)
# =============================================================================
# These are automatically set by Replit and don't need manual configuration:
# - PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE
# - REPLIT_ENVIRONMENT, REPL_ID, REPLIT_DOMAINS

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env in your project root
# 2. Replace placeholder values with your actual API keys
# 3. For Geoapify: Sign up at geoapify.com and copy your API key
# 4. For session secret: Generate a random string (e.g., openssl rand -base64 32)
# 5. For database: Use your PostgreSQL connection details or Replit's built-in database